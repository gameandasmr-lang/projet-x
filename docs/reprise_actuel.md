# État courant du projet — Projet X

> Mémoire de reprise immédiate de ce projet — même mécanisme que `The Long Ride/PROJECT/docs/reprise_actuel.md`.
> Dernière mise à jour : 2026-07-23 (session 6)

---

## Étape du projet

**Ouverture du chantier (2026-07-20)** : Arnaud a mis The Long Ride en pause volontaire pour se former à la pratique du développement/production sur un premier jeu délibérément simple, réalisable en 2-3 mois. Décision motivée par plusieurs retours convergents de professionnels du jeu vidéo rencontrés (voir `The Long Ride/PROJECT/docs/actif/retours_professionnels/`) : commencer petit avant un projet ambitieux.

**Pivot de concept (2026-07-20, session 1)** : la piste "Le Chasseur Inversé" est abandonnée (gardée en historique dans `docs/CONCEPT_JEU.md`). Nouvelle vision apportée par Arnaud : jeu du **Nécromancien** (invocations, village à conquérir, méta-progression, monde procédural à terme). Vision complète actée comme référence long terme ; **périmètre V1 (2-3 mois) validé** : nécromancien jouable, 2-3 invocations simples en contrôle RTS-lite, mana, orbes = petite méta-progression, niveaux construits à la main (pas de génération procédurale), pas de contre-types en V1.

**État actuel (2026-07-20, session 2) — gouvernance + descente mécaniques.**
- Gouvernance TLR distillée et actée dans `CLAUDE.md` (`<gouvernance>`) : source unique de vérité par sujet, distinction validé/en réflexion, pas de réécriture silencieuse d'une décision actée — sans reprendre l'appareil complet de TLR (pas d'IDs, pas de cycle en 6 étapes, pas de tags de confiance).
- Moteur confirmé : **GDevelop** (continuité TLR). Skill `.claude/Skills/gdevelop-operations/` créé — littératie moteur générique extraite des skills GDevelop de TLR, purgée de tout contenu métier TLR.
- Descente du concept dans `CONCEPT_JEU.md` : **score = orbes** (une seule monnaie) ; fin de niveau = sortie débloquée + fenêtre de 10s avec vagues intensifiées (pas d'arrêt automatique) ; **économie de mana** première passe chiffrée (mana max 100, régén 5/sec, coût croissant Gobelin 15/Démon 30/Colosse 50, cooldown qui suit le coût — ⚠️ à valider en playtest) ; **3 niveaux** confirmés pour la V1 (village / centre+château / couloirs+sommet, fusion de la progression à 5 étapes de la vision complète).

**État actuel (2026-07-22, session 3) — design concret des 3 niveaux acté.**
- Bases communes fixées : caméra qui suit le nécromancien (pas d'écran fixe), échelle héritée de TLR (grille 32×32, perso ~128px), vitesse 6 tiles/s, 1 kill = 1 orbe, seuil de sortie = 70% des orbes tuables, ennemis en mix socle fixe + vagues de spawn.
- Les 3 niveaux chiffrés en première passe (dimensions, nombre/placement d'ennemis, vagues) dans `CONCEPT_JEU.md` section "Design concret des 3 niveaux" — ⚠️ tous les chiffres à valider en playtest, même statut que l'économie de mana.
- Niveau 2 introduit les **obstacles défensifs** (bloquent déplacement + ligne de vue). Niveau 3 introduit le **boss final**, qui incarne la fenêtre de sortie intensifiée déjà décidée (le joueur fuit vers la sortie pendant que le boss le poursuit, plutôt qu'un combat séparé).
- **Méta-progression fermée** : liste des 2 améliorations permanentes actée (cooldown -20% à 100 orbes, PV max +25% à 150 orbes) — scope V1 "1-2 améliorations" désormais complet, plus de contenu à ajouter ici sans revalidation.

**État actuel (2026-07-22/23, session 4) — implémentation GDevelop démarrée, socle mouvement + spawn Gobelin validé.**
- Projet GDevelop créé en local (`PROJECT/game/Projet X.json`, stockage local — pas cloud, limite de 3 projets cloud atteinte sur le compte). 1280×720, pixel art activé (nearest neighbor).
- Scène `Niveau1_Village` (scène de démarrage) : `Necromancien` (Sprite placeholder violet, `TopDownMovementBehavior` à 192 px/s / 6 tiles/s conforme au concept), déplacement en **ZQSD** (contrôles par défaut désactivés, touches simulées individuellement), caméra qui suit via `CenterCameraOnObject`.
- **Invocation Gobelin** : clic gauche fait apparaître un `Gobelin` (placeholder vert, encore statique — pas de comportement/IA) à la position du curseur, si Mana ≥ 15 et cooldown terminé (2s) ; consomme le mana et relance le cooldown. Pas encore de régénération passive de mana, ni de sélection par touche 1/2/3 (un seul type d'invocation existe pour l'instant, donc pas encore nécessaire).
- Variables globales posées : `Mana`, `ManaMax`, `CoutGobelin`, `CooldownGobelin`, `CooldownGobelinDuree`.
- **Repères visuels** : 8 marqueurs de décor statiques (`RepereDecor`, gris, sans comportement) dispersés autour du spawn, pour visualiser le défilement de la caméra en l'absence de vrai décor.
- **Debug** : calque `Debug` dédié (caméra indépendante, non suivie) avec un texte `TxtMana` affichant Mana actuel/max en continu — respecte la règle de séparation des couches du skill GDevelop (jamais de debug dans le calque de simulation/HUD).
- Tout testé et validé par Arnaud dans l'éditeur, plus aucun bloc d'événement en rouge.
- Pas encore de décor réel/tiles, pas d'IA d'invocation (suivi du mage, ordre d'attaque), pas d'ennemis.

**Suite (2026-07-23, même session) — comportement de horde du Gobelin.**
- Le Gobelin invoqué se dirige maintenant vers le nécromancien (`AddForceTowardObject`, 250 px/s) et s'arrête à 50px (condition `Distance` inversée). Plusieurs allers-retours de debug ont été nécessaires : la condition `Distance` a un sens fixe ("est en dessous de X pixels", pas de paramètre de signe) — l'inversion se fait via le flag `inverted` de la condition, pas un paramètre. Détail capitalisé dans le skill `gdevelop-operations` (nouvelle section "Identifiants d'instructions vérifiés").
- Séparation entre Gobelins ajoutée pour éviter qu'ils s'empilent (`SeparateFromObjects`, Gobelin contre lui-même) — fonctionne, même si certains finissent encore par chevaucher le nécromancien lui-même (pas encore de séparation Gobelin/Necromancien, à améliorer si ça gêne en pratique).
- Le skill GDevelop du projet contient maintenant une table de référence des identifiants d'instructions déjà vérifiés en conditions réelles (nom exact, paramètres, pièges) — à consulter en priorité avant de redeviner un nom d'action/condition.

**Suite (2026-07-23, session 5) — premiers ennemis + organisation du projet en dossiers/groupes.**
- **Villageois** ajouté : Sprite placeholder statique (marron 32×32, sans comportement), 3 instances en cluster dans `Niveau1_Village` (~1050-1085, 450-495) pour servir de cible de test — pas encore de PV ni de logique de combat, juste une présence dans la scène.
- **Objets rangés en dossiers** dans le panneau objets : `Personnages` (Necromancien), `Invocations` (Gobelin), `Ennemis` (Villageois), `Décor` (RepereDecor), `UI` (TxtMana). Structure anticipée pour accueillir Démon/Colosse dans Invocations et héros/boss dans Ennemis plus tard.
- **Événements rangés en 5 groupes**, même ordre d'exécution qu'avant (aucune logique déplacée) : `Caméra`, `Déplacement Nécromancien`, `Invocation Gobelin`, `Debug/HUD`, `IA Gobelin`.
- Arnaud a créé les deux premiers exemples vides directement dans l'éditeur (dossier `Personnages`, groupe `Caméra`) pour valider le pattern JSON ; Claude Code a peuplé ces deux-là et répliqué le pattern sur le reste.
- Réorganisation dossiers/groupes rouverte par Arnaud dans l'éditeur : aucun bloc rouge, validée.

**Suite (2026-07-23, même session) — ordre d'attaque du Gobelin.**
- Nouveau groupe d'événements **`Ordre d'attaque`** (entre `Invocation Gobelin` et `Debug/HUD`) : variable d'objet `Ciblé` (0/1) sur Villageois ; clic droit sur un Villageois → reset de tous les `Ciblé` à 0 puis mise à 1 de celui cliqué (pattern reset-puis-filtre) ; tant qu'un Villageois est ciblé, tous les Gobelins foncent dessus (`AddForceTowardObject`, réutilisé tel quel) et ignorent le retour vers le nécromancien (condition `VarObjet` inversée ajoutée à l'event de horde dans `IA Gobelin`).
- **Décision de contrôle (2026-07-23)** : clic gauche = spawn Gobelin (systématique, quel que soit ce qu'il y a sous le curseur), clic droit = ordre d'attaque sur la cible sous le curseur. Remplace l'approche initiale (différenciation par ce qui est sous le curseur au clic gauche), plus simple et sans ambiguïté.
- 3 nouvelles instructions passées en conditions réelles sans bloc rouge, capitalisées dans le skill `gdevelop-operations` : `SourisSurObjet` (curseur sur objet), `VarObjet`/`ModVarObjet` (variable d'objet, même schéma que `VarGlobal`/`ModVarGlobal`).
- Pas encore de gestion de dégâts/mort sur Villageois — seul le déplacement de l'ordre d'attaque est implémenté à ce stade, comme prévu.
- **Bug trouvé et corrigé par Arnaud en playtest** : le retour vers la horde continuait de s'appliquer même avec un ordre actif. Cause racine : `VarObjet` inversé s'évalue instance par instance, pas comme un "aucune instance ne correspond" global — avec 3 Villageois, dès qu'un seul était ciblé, les 2 autres (toujours `Ciblé=0`) suffisaient à garder la condition inversée vraie. Corrigé en ajoutant une variable **globale** `CibleActive` (0/1) mise à jour en parallèle du tag par-objet, l'event de horde dans `IA Gobelin` teste maintenant `VarGlobal(CibleActive,=,0)` au lieu de l'inversion bugguée. Piège capitalisé dans le skill `gdevelop-operations`.
- Correction retestée et validée par Arnaud dans l'éditeur : plus aucune concurrence entre l'ordre d'attaque et le retour vers la horde.

**Suite (2026-07-23, même session) — régénération de mana, résolution de combat, ciblage de groupe.**
- **Régénération de mana** : nouveau groupe `Régénération Mana` (avant `Invocation Gobelin`), +5/sec plafonné à `ManaMax`, deux events (accumulation + clamp) avec seulement des instructions déjà vérifiées.
- **Résolution de combat** : nouveau groupe `Combat Gobelin` (après `Ordre d'attaque`). Villageois a maintenant `PV` (20 au départ) et `GroupeID` (1 par défaut, les 3 instances actuelles forment un seul groupe) ; Gobelin a `CooldownAttaque` par instance. Portée d'attaque 40px (le Gobelin s'arrête à cette distance au lieu de rentrer dans la cible), dégâts et cooldown d'attaque en globales (`DegatsGobelin`=5, `CooldownAttaqueGobelinDuree`=1s, premier chiffrage à ajuster en playtest). Mort du Villageois à PV≤0 → `Delete`.
- **Ciblage de groupe** : clic droit sur un Villageois cible tout son `GroupeID` (pas seulement l'instance cliquée) — mécanisme prêt pour plusieurs clusters, même si un seul existe pour l'instant. Retour automatique à la horde quand le groupe entier est éliminé (compteur `NbCiblesGroupe`, remis à 0 puis recompté chaque frame — évite le même piège d'inversion multi-instance que le bug précédent).
- **Annulation d'ordre** : déjà existante, confirmée à nouveau (clic droit sur le sol vide reset tout).
- **Un Gobelin ne combat qu'une seule cible à la fois** (demandé par Arnaud après un premier test où plusieurs Villageois du même groupe étaient frappés simultanément par un seul Gobelin à portée de plusieurs). Solution : event **`Pour chaque instance de Gobelin`** (`BuiltinCommonInstructions::ForEach`, nouveau type d'event, structure lue directement depuis un exemple créé par Arnaud dans l'éditeur plutôt que devinée) combiné à **`PickNearest`** (sélectionne le Villageois le plus proche de CE Gobelin précis, paramètres `[Objet, X, Y, ""]`) — remplace les events de poursuite et de dégâts qui s'appliquaient auparavant à toutes les paires Gobelin×Villageois simultanément. `PickNearest` pas encore confirmé en jeu à ce stade (structure lue avant qu'Arnaud ait renseigné les X/Y, corrigés ensuite côté JSON avec `Gobelin.X()`/`Gobelin.Y()`).
- **Bug trouvé et corrigé** : l'affichage du mana utilisait `Round(...)` (majuscule) → bloc rouge. Les fonctions mathématiques d'expression GDevelop sont en minuscules (`round`, `floor`, `ceil`...), contrairement au reste du moteur en PascalCase (`ToString`, `GlobalVariable`). Corrigé en `round(...)`, capitalisé dans le skill. Écrasé une fois par une sauvegarde d'Arnaud pendant l'échange, réappliqué.
- Idée notée mais pas implémentée : possibilité d'annuler un ordre pour déclencher un système d'aggro futur (les ennemis engagés se dirigent vers le nécromancien une fois l'ordre relâché) — piste explicitement "pour plus tard" selon Arnaud, liée à de futurs sorts de zone/pièges pour aider les invocations.
- ⚠️ Pas encore testé dans l'éditeur à ce stade (mana, combat, ciblage de groupe, ForEach/PickNearest) — à confirmer en premier à la prochaine session.

## Ouvert — pas pour maintenant

- **HUD de sélection** (demandé par Arnaud, 2026-07-23) : permettre au joueur de choisir quelle invocation faire naître (aujourd'hui : un seul type, Gobelin, donc pas encore nécessaire) et lesquelles envoyer à l'attaque (aujourd'hui : ordre d'attaque touche tous les Gobelins vivants, pas de sélection de sous-groupe). Rejoint le système de sélection par famille déjà décrit dans `CONCEPT_JEU.md` (touches 1/2/3, bascule défense/détachée) — à construire quand Démon/Colosse existeront et que la distinction aura un sens concret.
- **Système d'aggro/annulation stratégique** (idée d'Arnaud, 2026-07-23) : ordre annulé → ennemis engagés continuent d'attaquer (le nécromancien ou les invocations) au lieu de simplement s'arrêter. Lié à de futurs sorts de zone/pièges du nécromancien pour soutenir ses invocations. Explicitement une piste "pour plus tard", pas un design validé.
- **Reste du système d'aggro** (design discuté par Arnaud le 2026-07-23) : détection + propagation de groupe + fuite de base sont construites (voir détail de session). Restent : reset propre de `Aggro` (retour à un état passif si plus aucune menace proche pendant un moment — nécessite une technique de comptage, pas une inversion directe, même piège que `NbCiblesGroupe`) ; généralisation de la propagation par groupe au-delà des 2 `GroupeID` codés en dur ; réaction différenciée par type d'ennemi (pour l'instant seul le Villageois a une réaction, "la réponse dépend de qui ils sont" reste à construire pour les futurs types d'ennemis).

**Suite (2026-07-23, même session) — HUD cooldown Gobelin.**
- Nouvel objet `TxtCooldown` (calque Debug, sous `TxtMana`), affiche `Cooldown: X.Xs` (1 décimale, `round(GlobalVariable(CooldownGobelin)*10)/10`) — `0s` = prêt à invoquer. Event ajouté dans le groupe `Debug/HUD`, après celui de `TxtMana`.
- Arnaud confirme que le combat un-gobelin-une-cible semble fonctionner (difficile à juger à l'œil avec certitude, mais pas de bug visible signalé).
- Testé et validé par Arnaud dans l'éditeur : le HUD cooldown s'affiche et redescend à 0 correctement.

**Suite (2026-07-23, même session) — affichage dynamique des dégâts.**
- Nouvel objet `TxtDegats` (texte rouge/gras, pas d'instance placée dans la scène — créé uniquement à l'exécution, même pattern que Gobelin) rangé dans un nouveau dossier `Effets`. Variable d'objet `DureeVie` (1 seconde par défaut).
- Au moment d'un coup porté (event `ForEach Gobelin` de dégâts dans `Combat Gobelin`) : `Create` fait apparaître un `TxtDegats` au-dessus du Villageois touché (`Villageois.X()` / `Villageois.Y()-40`), puis son texte est mis au montant de `DegatsGobelin`.
- Décompte de `DureeVie` chaque frame, suppression (`Delete`) une fois à 0 — même mécanique de compte à rebours que `CooldownAttaque`/`CooldownGobelin`.
- **Zéro nouvelle instruction non vérifiée** : entièrement construit avec `Create`, `TextObject::String`, `VarObjet`/`ModVarObjet`, `Delete` — tous déjà confirmés dans ce projet. Pas de suivi de la cible ni de fondu (le nombre reste fixe à sa position de spawn puis disparaît net) — simplification volontaire, améliorable plus tard si besoin.
- Forme réelle de `Delete` confirmée par l'éditeur : paramètres `[NomObjet, NomObjet]` (nom répété), différent de la première tentative — capitalisé dans le skill.
- Testé et validé par Arnaud : le nombre de dégâts s'affiche bien au bon endroit.

**Suite (2026-07-23, même session) — ajustement affichage dégâts + vibration en combat.**
- **Durée du texte de dégâts réduite** : 1s → 0.6s (plus court que le cooldown d'attaque de 1s, ne chevauche plus le coup suivant).
- **Texte de dégâts lié à la mort de sa cible** : juste avant la suppression du Villageois (PV≤0), un event cherche s'il y a un `TxtDegats` à moins de 60px et le supprime immédiatement (`Distance` + `Delete`, déjà vérifiés) — le texte ne survit plus à la mort de l'ennemi qui l'a généré, sans avoir besoin de lier les deux objets.
- **Vibration en combat** (Gobelin uniquement pour l'instant) : nouveau groupe `Vibration Gobelin` (entre `Combat Gobelin` et `Debug/HUD`). 4 nouvelles variables d'objet sur Gobelin (`EnCombat`, `AncrageX`, `AncrageY`, `TempsVibration`). Dès qu'un Gobelin entre à portée d'attaque, sa position est mémorisée comme ancrage ; tant qu'il combat, sa position oscille autour de cet ancrage via `SetX`/`SetY` + `sin`/`cos` (petit mouvement elliptique ~3px, sans dérive dans le temps car toujours calculé par rapport au point fixe, jamais additionné frame par frame). Flag remis à 0 (arrête la vibration) soit quand la cible sort de portée, soit quand l'ordre d'attaque est entièrement annulé.
- **Structure de `SetX` confirmée** via un exemple créé par Arnaud dans l'éditeur : `["NomObjet", opérateur, expression]`, capitalisée dans le skill. `SetY`, `sin()`, `cos()` construits par extension/hypothèse (mêmes conventions que `SetX`/`round()`) mais pas encore confirmés en jeu.
**Suite (2026-07-23, même session) — combat retour du Villageois, rééquilibrage, deuxième groupe de test.**
- **Décision actée** : le Villageois riposte désormais (il ne subissait que des dégâts jusqu'ici). Confirmé explicitement par Arnaud après une question de clarification (l'alternative "vibration cosmétique seulement, sans riposte" a été explicitement écartée).
- **Rééquilibrage** : Villageois nettement plus faible que le Gobelin, comme demandé — PV Villageois 20→**10** ; Gobelin gagne des PV (**15**, nouveau, n'existait pas avant) ; dégâts Villageois→Gobelin très faibles (`DegatsVillageois`=**2**, cooldown `CooldownAttaqueVillageoisDuree`=**1.5s**) contre dégâts Gobelin→Villageois inchangés (5, cooldown 1s). Un Gobelin tue un Villageois en 2 coups ; un Villageois seul met ~8 coups pour tuer un Gobelin — cohérent avec "faibles mais nombreux".
- **Nouveau groupe `Combat Villageois`** (après `Combat Gobelin`) : décrément du cooldown d'attaque du Villageois, dégâts au Gobelin le plus proche (`ForEach Villageois` + `PickNearest` sur Gobelin, même schéma miroir que `Combat Gobelin`), texte de dégâts au-dessus du Gobelin touché, suppression du Gobelin à PV≤0 (avec nettoyage du texte de dégâts proche avant, même pattern que pour le Villageois).
- **Texte de dégâts avec signe** : affiche maintenant `-5` / `-2` (préfixe `"-"` ajouté), des deux côtés.
- **Deuxième groupe de test** : 10 nouveaux Villageois (`GroupeID`=2, distinct du premier cluster) ajoutés autour de (650-770, 600-680), à l'écart du premier groupe — pour tester le combat contre une horde nombreuse plutôt qu'un cluster de 3.
- **Vibration étendue au Villageois** : nouveau groupe `Vibration Villageois` (après `Vibration Gobelin`), miroir exact du mécanisme Gobelin (mêmes 4 variables, mêmes events, `Gobelin`/`Villageois` inversés). Se déclenche quand le Villageois est `Ciblé`=1 et un Gobelin à portée.
- Testé et validé par Arnaud dans l'éditeur : combat retour, rééquilibrage, 2 groupes de Villageois, vibration des deux côtés — tout fonctionne bien.
- Confirmation de principe d'Arnaud : le Villageois est l'unité la plus faible du jeu, quasi insignifiante seule — n'a d'utilité qu'organisé en groupe nombreux. Principe à garder en tête pour l'équilibrage des futurs ennemis/niveaux.

**Suite (2026-07-23, même session) — première brique d'un système d'aggro (détection seulement).**
- **Design discuté avec Arnaud** : un système d'aggro plus large est envisagé — proximité du nécro/d'une invocation déclenche l'aggro d'un groupe, réaction dépendante du type d'ennemi (les Villageois fuient vers une position de sécurité, plus lentement que les invocations, mais une fois le combat engagé le groupe entier reste combattre ; groupe aggro se déplace toujours ensemble, jamais un membre isolé). **Décision explicite** : construire par étapes testables plutôt que tout d'un coup — cette session ne couvre que la détection + indicateur visuel, pas la fuite ni le mouvement de groupe (prévu comme prochaine brique, liée entre elles).
- **Détection d'aggro implémentée** : nouveau groupe `Aggro Villageois` (après `Vibration Villageois`). Variable d'objet `Aggro` (0/1) sur Villageois : passe à 1 si le Nécromancien OU un Gobelin est à moins de 150px (portée d'aggro, supérieure à la portée de combat de 40px). Indicateur visuel **`TxtAggro`** ("!" jaune, gras) créé au-dessus du Villageois dès qu'il devient aggro (variable `IconAggroCree` pour éviter les créations répétées).
- **Simplifications assumées pour cette première brique** (à corriger avec la fuite) :
  - Pas de reset de `Aggro` une fois déclenché (reste aggro indéfiniment pour l'instant) — un reset propre nécessiterait un test "aucune menace à proximité", qui a le même risque d'inversion multi-instance que le bug `VarObjet` de la session 5 ; à traiter proprement avec le système de fuite plutôt que rafistolé maintenant.
  - Pas de propagation à tout le `GroupeID` — seuls les Villageois directement proches du nécro/Gobelin passent en `Aggro`, pas encore tout leur groupe. La propagation groupée a plus de sens une fois la fuite cohésive construite (c'est elle qui a vraiment besoin que "tout le groupe" soit synchronisé).
  - Zéro nouvelle instruction non vérifiée : entièrement construit avec `Distance`, `VarObjet`/`ModVarObjet`, `Create` — tous déjà confirmés.
- Testé et validé par Arnaud dans l'éditeur : le "!" apparaît bien.
- **Ajustement** : `TxtAggro` disparaît maintenant après 0.8s (variable `DureeVie` + décompte + `Delete`, même mécanique que `TxtDegats`) — c'est un flash d'alerte ponctuel, pas une icône de statut permanente. `Aggro` reste à 1 en interne (pas de reset), seul l'affichage est temporaire.
- Testé et validé par Arnaud dans l'éditeur.

**Suite (2026-07-23, même session) — les Gobelins ne chevauchent plus le Nécromancien.**
- Nouvel event dans `IA Gobelin` : `SeparateFromObjects(Gobelin, Necromancien)` dès que la distance passe sous 45px — même instruction déjà utilisée entre Gobelins, juste appliquée en plus au Nécromancien. Zéro nouvelle instruction.
- Combiné à l'event existant (approche stoppée à 50px), ça crée une zone stable 45-50px autour du nécro : les Gobelins s'approchent puis se repoussent mutuellement dans cette bande, ce qui donne naturellement un anneau autour de lui plutôt qu'un empilement — sans algorithme de placement en cercle explicite.
- ⚠️ Pas encore testé dans l'éditeur à ce stade.

**Suite (2026-07-23, même session) — écart plus grand + fin de l'effet de vibration en cas de blocage.**
- **Seuils agrandis** pour un écart plus visible : approche stoppée à 65px (au lieu de 50), séparation Gobelin/Necromancien déclenchée à 55px (au lieu de 45) — bande stable 55-65px.
- **Fin du "forcing" visuel** : un Gobelin bloqué par un voisin (à moins de 35px d'un autre Gobelin) n'essaie plus de continuer à avancer vers le nécro. Nouvelle variable d'objet `Bloque` sur Gobelin, remise à 0 puis recalculée chaque frame via `Distance(Gobelin,Gobelin,35)` **non inversée** (délibérément — inverser cette condition aurait le même piège multi-instance que le bug `VarObjet` de la session 5, donc plutôt qu'inverser, on calcule le drapeau dans le bon sens puis on le consulte tel quel). L'event d'approche vers le nécro ajoute la condition `VarObjet(Gobelin,Bloque,=,0)`.
- Zéro nouvelle instruction non vérifiée — uniquement `Distance`, `VarObjet`/`ModVarObjet`, déjà confirmés.
- **Bug trouvé par Arnaud en playtest** : `Bloque` se déclenchait aussi pendant le trajet de retour vers le nécro (pas seulement une fois arrivé près de lui) — un groupe de Gobelins revenant ensemble après un combat pouvait se bloquer mutuellement en chemin et ne jamais atteindre le nécro, restant plantés à mi-chemin indéfiniment. Corrigé en ajoutant une condition `Distance(Gobelin,Necromancien,120)` à l'event qui déclenche `Bloque` — un Gobelin ne se considère bloqué que s'il est à la fois près d'un voisin ET déjà proche du nécro (dans sa phase d'approche finale), jamais pendant le trajet de retour.
- Testé et validé par Arnaud dans l'éditeur : les Gobelins reviennent bien tous vers le nécro après un combat, plus de blocage à mi-chemin.

**Suite (2026-07-23, même session) — première brique de la fuite groupée des Villageois.**
- **Propagation de l'Aggro à tout le groupe** : 2 variables globales `Groupe1Aggro`/`Groupe2Aggro` (une par `GroupeID` existant) — si un membre du groupe est `Aggro`, le flag global du groupe passe à 1, puis tous les membres de ce `GroupeID` sont mis à `Aggro=1`. ⚠️ **Codé explicitement pour les groupes 1 et 2** (les seuls qui existent) — pas encore générique pour un nombre quelconque de groupes, à revoir si plus de clusters sont ajoutés plus tard (probablement via une variable structure/tableau plutôt qu'une globale par groupe).
- **Nouveau groupe `Fuite Villageois`** : un Villageois `Aggro=1` et **pas encore** `Ciblé` (pas encore engagé dans un vrai combat) s'éloigne du Nécromancien. S'arrête de fuir au-delà de 300px (sinon il fuirait indéfiniment) et dès que `Ciblé=1` (le combat prend le dessus sur la fuite, conforme à "une fois le combat lancé ils restent tous combattre").
- Vitesse de fuite (100 px/s) volontairement plus lente que la poursuite des Gobelins (250 px/s), comme demandé.
- Pas de reset de `Aggro` implémenté (toujours la même simplification qu'à la détection) — une fois fui à 300px, le Villageois s'arrête et reste `Aggro` indéfiniment sans revenir à un état passif.
- Testé par Arnaud : la fuite fonctionne, mais chaque Villageois partait dans une direction différente (éventail) au lieu de partir en bloc — chacun calculait sa propre direction "à l'opposé du nécro" depuis sa position.

**Suite (2026-07-23, même session) — 2 corrections : fuite en bloc + rejoindre le combat.**
- **Fuite en éventail corrigée** : remplacé `AddForceTowardObject` (direction calculée par instance, cause du problème) par une **direction figée et partagée par tout le groupe**, calculée une seule fois à la première transition `GroupeXAggro` 0→1 (vecteur normalisé nécro→Villageois déclencheur, stocké dans 4 nouvelles globales `Groupe1FuiteDX/DY`, `Groupe2FuiteDX/DY`) puis appliquée à tous les membres via `SetX`/`SetY` (`+=direction*vitesse*TimeDelta()`) — tout le groupe se déplace maintenant en parallèle plutôt qu'en éventail. Réutilise uniquement des instructions déjà confirmées, plus `sqrt()` (hypothèse non testée, même convention minuscule que `round`/`sin`/`cos`).
- **Villageois rejoignent le combat** : un Villageois `Ciblé=1` mais pas encore à portée d'un Gobelin restait immobile pendant que ses camarades combattaient et mouraient un par un. Nouvel event `ForEach Villageois` en tête du groupe `Combat Villageois` (miroir du même mécanisme que l'approche du Gobelin) : si `Ciblé=1` et pas encore à portée (40px) du Gobelin le plus proche, le Villageois avance vers lui (vitesse 100). Zéro nouvelle instruction.
- ⚠️ Pas encore testé dans l'éditeur à ce stade (les deux corrections).
- Testé par Arnaud : le mouvement de groupe (fuite en bloc + ralliement au combat) fonctionne bien. Mais retour sur le déclencheur : donner l'ordre d'attaque (`Ciblé=1`) ne doit **pas** à lui seul faire basculer le groupe en mode combat — les Villageois doivent continuer à fuir le Gobelin qui approche, et **seul un coup réellement encaissé** doit faire basculer tout le groupe en réponse.

**Suite (2026-07-23, même session) — séparation de `Ciblé` (ordre du joueur) et `GroupeAuCombat` (combat réellement engagé).**
- **Problème** : `Ciblé` servait à la fois à dire aux Gobelins qui poursuivre ET à faire basculer les Villageois en mode combat (arrêt de la fuite, riposte, ralliement) — dès que l'ordre était donné, tout basculait instantanément, avant même qu'un Gobelin ait touché qui que ce soit.
- **Nouvelle variable d'objet `GroupeAuCombat`** sur Villageois (0/1, jamais remise à 0 — même simplification que `Aggro`) : mise à 1 uniquement sur le Villageois qui vient réellement d'encaisser un coup (action ajoutée dans l'event de dégâts du groupe `Combat Gobelin`), puis propagée à tout son `GroupeID` (2 variables globales `Groupe1Combat`/`Groupe2Combat`, même schéma de propagation que `Aggro` — trigger sur la 1ère transition + broadcast).
- **`Ciblé` garde son seul rôle d'origine** : dire aux Gobelins qui poursuivre (`Ordre d'attaque`, `Combat Gobelin` inchangés).
- **`GroupeAuCombat` pilote maintenant** : l'arrêt de la fuite (`Fuite Villageois`, condition `Ciblé=0`→`GroupeAuCombat=0`), le ralliement des Villageois pas encore au contact (`Combat Villageois`, condition `Ciblé=1`→`GroupeAuCombat=1`), et la riposte (même changement) — cohérent avec "une fois le combat lancé ils restent tous combattre" : même si le joueur annule l'ordre après coup, un groupe qui a réellement encaissé un coup reste engagé (pas de reset de `GroupeAuCombat`).
- Zéro nouvelle instruction non vérifiée — réutilise le schéma de propagation déjà construit pour `Aggro`.
- ⚠️ Pas encore testé dans l'éditeur à ce stade.

**Suite (2026-07-23, même session) — validation combat/fuite + séparation Villageois/Villageois.**
- Arnaud confirme en playtest : la séparation `Ciblé`/`GroupeAuCombat` fonctionne comme prévu — un groupe fuit bien un Gobelin qui approche même après un ordre d'attaque donné, et ne bascule en combat qu'au moment où un coup est réellement encaissé. Aucun bloc rouge signalé, `sqrt()` donc confirmé en jeu (capitalisé dans le skill, plus de `⚠️`).
- **Bug résiduel signalé** : les Villageois d'un même groupe finissent par se chevaucher entre eux (pas de séparation Villageois/Villageois, contrairement aux Gobelins qui en ont déjà une entre eux).
- **Corrigé** : nouveau groupe **`Séparation Villageois`** (fin de la liste des groupes de la scène, après `IA Gobelin`) — `SeparateFromObjects(Villageois, Villageois)` sous 30px, même instruction déjà vérifiée que pour les Gobelins. Zéro nouvelle instruction non vérifiée.
- Testé et validé par Arnaud dans l'éditeur : moins de chevauchement, comportement amélioré.

**Suite (2026-07-23, même session) — brique orbes + sortie de niveau + doc de navigation JSON.**
- Nouvelle brique "fermer la boucle du niveau 1" (choisie après discussion sur le rythme du projet vs délai 2-3 mois) : orbes sur kill de Villageois (compteur global `Orbes`), seuil de sortie `OrbesSeuil`=9 (70% des 13 Villageois actuels en scène — vagues de spawn explicitement reportées à une session suivante, brique séparée). Nouvel objet `Sortie` (placeholder, bord est du village) et nouveau groupe `Sortie Niveau` : déblocage au seuil, texte de statut `TxtSortieStatut`, fin de niveau (`NiveauTermine`) à l'approche du Nécromancien une fois débloquée → `TxtNiveauTermine`. HUD `TxtOrbes` ajouté dans `Debug/HUD`.
- Fenêtre de 10s + vagues intensifiées (déjà actées dans `CONCEPT_JEU.md`) explicitement pas implémentées ici — regroupées avec les vagues de spawn en attente.
- Testé et validé par Arnaud dans l'éditeur : tout fonctionne.
- **Nouveau doc `docs/structure_json_projet_x.md`** : carte de navigation du JSON (liste des 15 groupes de la scène dans l'ordre d'exécution + rôle, raccourcis, variables globales) — créé après une hésitation initiale (attendre un besoin réel vs anticiper), tranchée en faveur de la création immédiate vu le besoin déjà récurrent de re-localiser des groupes par grep. Version scalée à la taille actuelle du projet (groupes à plat, une seule scène), pas l'arborescence profonde de TLR. À tenir à jour à chaque ajout/déplacement/renommage de groupe.

**Suite (2026-07-23, même session) — vagues de spawn du niveau 1.**
- **Chiffrage aligné sur `CONCEPT_JEU.md`** : 13 Villageois fixes déjà en scène + 2 vagues de 7 = total tuable 27 (dans la fourchette ≈27-28 visée). `OrbesSeuil` relevé de 9 à **19** (70% de 27, conforme au chiffrage).
- **Nouveau groupe `Vagues Niveau 1`** (entre `Séparation Villageois` et `Sortie Niveau`) : Vague 1 (7 Villageois) à `Orbes>=8` (~30%), près du cluster 1 existant ; Vague 2 (7 Villageois) à `Orbes>=16` (~60%), près du cluster 2. Flags `Vague1Faite`/`Vague2Faite` pour ne spawner qu'une fois.
- **Décision de réutilisation des groupes existants** (pas de nouveaux `GroupeID`) : les Villageois de vague rejoignent `GroupeID` 1 ou 2 plutôt que de créer des groupes 3/4, pour éviter le problème déjà identifié (propagation d'aggro/combat codée en dur pour les groupes 1 et 2 seulement, pas générique). Vérifié que la propagation (`Aggro Villageois`, `Combat Villageois`) tourne chaque frame par `GroupeID`, donc les renforts rejoignent automatiquement l'état courant du groupe (aggro/fuite/combat déjà engagé) dès leur apparition — cohérent.
- **Point non vérifié à ce stade** : la Vague 2 a besoin d'un `ModVarObjet GroupeID=2` appliqué après 7 `Create` d'affilée (la Vague 1 n'a besoin de rien, le `GroupeID`=1 par défaut suffit). Nouveau pattern jamais testé dans ce projet (jusqu'ici, un seul `Create` puis une action, jamais plusieurs `Create` du même objet suivis d'une action groupée) — capitalisé dans le skill comme point à vérifier en priorité si un seul Villageois sur 7 récupère le bon groupe après la vague 2.
- ⚠️ Pas encore testé dans l'éditeur à ce stade.

- Testé et validé par Arnaud dans l'éditeur : les renforts apparaissent bien et rejoignent correctement leur groupe (7/7, pas seulement le dernier créé) — pattern multi-`Create` + action groupée confirmé, capitalisé dans le skill.

**Suite (2026-07-23, même session) — PV du Nécromancien, attaque directe des renforts, Game Over.**
- **Retour d'Arnaud après le test des vagues** : dans le design final, les renforts devraient sortir de maisons (le joueur ne sachant jamais de laquelle) — noté dans "Ouvert" ci-dessous, pas implémentable tant que le vrai décor/les maisons n'existent pas. En attendant, **décision actée** : les renforts sont toujours en mode combat direct contre le Nécromancien dès leur apparition (pas de phase de fuite/détection, ignorent les Gobelins). Ça impliquait d'ajouter la capacité pour un Villageois d'attaquer et tuer le Nécromancien → Game Over.
- **Nécromancien** : nouvelle variable `PV` (100 par défaut, première passe comme le reste des chiffres de combat).
- **Nouvelle variable d'objet `AttaqueDirecte`** sur Villageois (0/1) : complètement indépendante du système `Aggro`/`GroupeAuCombat`/`Ciblé` existant — un Villageois avec `AttaqueDirecte`=1 fonce sur le Nécromancien et l'attaque directement, sans jamais passer par la fuite ni cibler un Gobelin. **Décision explicite** : ne pas réutiliser le système d'aggro/fuite existant pour les renforts (qui aurait fait basculer tout le cluster GroupeID 1/2 en mode combat via la propagation déjà en place, même les membres du cluster non concernés par le renfort) — trop d'effet de bord non demandé, plus simple et plus prévisible en tant que comportement séparé.
- Les 2 vagues (`Vagues Niveau 1`) mettent maintenant `AttaqueDirecte`=1 sur les 7 Villageois créés, en plus du `GroupeID` déjà en place (gardé pour le comptage/bookkeeping, mais n'influence plus leur comportement de combat).
- **Nouveau groupe `Attaque Necro`** : approche (`AddForceTowardObject`, 100px/s, identique à l'approche normale des Villageois), dégâts au contact (40px, mêmes `DegatsVillageois`/`CooldownAttaqueVillageoisDuree` réutilisés, texte de dégâts au-dessus du Nécro), et déclenchement de `GameOver`=1 + texte `TxtGameOver` ("GAME OVER") quand PV Nécro ≤ 0.
- **HUD** : `TxtPVNecro` ajouté dans `Debug/HUD` (lecture via `Necromancien.Variable(PV)`, pattern déjà confirmé par la vibration Gobelin/Villageois).
- **Portée volontairement pas étendue** : seuls les Villageois en `AttaqueDirecte`=1 (les renforts) peuvent viser/tuer le Nécromancien pour l'instant — les Villageois des clusters de base (1 et 2) continuent de ne cibler que les Gobelins, même s'ils sont au contact du Nécro. Point ouvert ci-dessous si Arnaud veut étendre.
- Zéro nouvelle instruction non vérifiée.
- ⚠️ Pas encore testé dans l'éditeur à ce stade.

## Ouvert — pas pour maintenant (suite)

- **Point de spawn des renforts imprévisible** (demandé par Arnaud, 2026-07-23) : dans le design final, les vagues devraient sortir de maisons/points de décor, sans que le joueur sache à l'avance de laquelle — nécessite un vrai décor avec des maisons identifiables, pas juste des `RepereDecor`. Pas implémentable tant que le pipeline décor/tiles n'est pas construit (toujours pas commencé à ce stade).
- ~~Les clusters de base peuvent-ils aussi attaquer le Nécro ?~~ **Tranché (2026-07-23)** : oui, même règle pour tous les Villageois engagés (voir suite de session ci-dessous) — le Nécro vise en priorité, repli sur un Gobelin bloquant au contact.
- **Capacité d'entassement autour du Nécro** (retour d'Arnaud après playtest, 2026-07-23) : souhait d'au moins 8 Villageois pouvant l'attaquer simultanément. Avec les seuils actuels (portée d'attaque 40px, séparation 35px), la géométrie du cercle autour du Nécro permet un peu moins que ça. Arnaud juge que c'est **acceptable pour l'instant** — piste évoquée pour plus tard : réduire la taille des sprites/hitbox au global pour desserrer la contrainte géométrique, plutôt que retoucher les seuils un par un. Pas d'action requise maintenant.

**Suite (2026-07-23, même session) — unification du comportement de combat Villageois (renforts = clusters de base).**
- **Retour d'Arnaud** : la règle doit être la **même** pour les renforts et les Villageois déjà présents (pas un système séparé) ; une fois en mode combat, un Villageois vise le Nécromancien, mais **riposte contre un Gobelin qui le bloque au contact** plutôt que de foncer à travers lui ; plusieurs Villageois doivent pouvoir s'entasser autour du Nécro pour le frapper à plusieurs, sans se superposer.
- **Système `AttaqueDirecte` de la session précédente supprimé** (variable retirée, groupe `Attaque Necro` réduit à son seul rôle Game Over, renommé) — remplacé par une généralisation directe de `Combat Villageois` : tout Villageois `GroupeAuCombat`=1 (renfort ou cluster de base, même logique) vise maintenant le Nécromancien en priorité, et ne riposte contre un Gobelin que si celui-ci est au contact (40px).
- **Renforts simplifiés** : les vagues mettent directement `GroupeAuCombat`=1 sur les 7 Villageois créés (au lieu de l'ancien `AttaqueDirecte`=1) — "par défaut en mode combat" dès l'apparition, comme demandé. ⚠️ **Effet de bord à surveiller** : comme `GroupeAuCombat`=1 sur un membre d'un `GroupeID` propage l'état à tout le groupe (mécanisme déjà en place depuis la session précédente), l'arrivée d'une vague de renforts fait aussi basculer en combat le cluster de base auquel elle est rattachée (même les membres pas encore engagés). Pas corrigé — pas mentionné comme problème par Arnaud, mais à surveiller au playtest si ça semble prématuré/étrange.
- **Nouvelle variable d'objet `GobelinBloque`** sur Villageois (0/1, recalculée chaque frame par reset+recalcul, jamais par inversion directe) : évite un piège potentiel non testé (condition `Distance`/`PickNearest` inversée sur un objet qui peut avoir zéro instance, ex. plus aucun Gobelin vivant) — capitalisé dans le skill à côté du piège `VarObjet` inversé déjà connu. Un Villageois avance vers le Nécro et l'attaque si `GobelinBloque`=0 ; sinon il reste sur place et attaque le Gobelin bloquant (logique déjà existante, inchangée).
- **Anti-chevauchement Villageois/Nécromancien** : ajouté dans `Séparation Villageois` (`SeparateFromObjects` sous 35px, approche stoppée à 40px) — même principe que le fix Gobelin/Nécromancien de la session précédente, pour que plusieurs Villageois puissent encercler le Nécro sans se superposer sur lui.
- Zéro nouvelle instruction non vérifiée.
- Testé et validé par Arnaud dans l'éditeur : tout est bien intégré (aucun bloc rouge, ciblage Nécro/repli Gobelin, entassement sans superposition). L'effet de bord "vague active tout le cluster de base" n'a pas été signalé comme gênant.

**Suite (2026-07-23, session 6) — Priorité 1 : fenêtre de sortie 10s + horde de Game Over.**
- **Décisions actées par Arnaud avant implémentation** (2 questions posées) : (1) échec de la fenêtre de 10s = Game Over, mis en scène par une horde surpuissante (PV/dégâts colossaux) qui converge sur le Nécromancien — explicitement **pas** des Villageois (nouveau type d'unité demandé) ; (2) intensification pendant la fenêtre = une vague ponctuelle unique de renforts Villageois au moment du déblocage (même pattern que les vagues existantes), distincte du mécanisme de horde à l'expiration.
- **Nouvel objet `Horde`** créé (dossier `Ennemis`) : nouveau placeholder `game/assets/horde_placeholder.png` généré (32×32, violet très sombre, via `System.Drawing` en PowerShell — même méthode probable que les 4 placeholders existants), enregistré comme ressource et référencé dans l'objet. Variable unique `CooldownAttaque`. **Volontairement non attaquable** (pas de PV, aucune logique de dégâts entrants) : le combat contre elle n'est pas simulé, cohérent avec "impossible à combattre" plutôt que de construire un système de combat pour une issue de toute façon jouée d'avance (principe directeur "finir vite").
- **Nouveau groupe `Fenêtre de Sortie`** (#18, après `Sortie Niveau`) :
  - Au déblocage de la sortie (`SortieDeverrouillee` 0→1) : démarre `FenetreSortieActive`=1, `FenetreSortieTemps`=`FenetreSortieDuree`(10s), spawn de 6 Villageois près de l'objet `Sortie` (positions relatives `Sortie.X()/Y()+offset`, jamais fait avant — jusqu'ici les vagues utilisaient des coordonnées absolues codées en dur) avec `GroupeAuCombat`=1 direct (même pattern que les renforts de `Vagues Niveau 1`).
  - Décompte du timer + affichage `TxtFenetreSortie` ("Sortie dans : X.Xs", même format que `TxtCooldown`).
  - À expiration (`FenetreSortieTemps`≤0) sans niveau terminé : spawn de 10 `Horde` en cercle autour du Nécromancien — positions calculées à la main (10 offsets à 36° d'écart, rayon 90px) plutôt que via `sin()`/`cos()` en dur au runtime, pour zéro dépendance à une fonction trigonométrique sur ce point précis (déjà confirmées ailleurs dans le projet, mais superflu ici puisque les 10 points sont fixes).
- **Nouveau groupe `Combat Horde`** (#19) : chaque Horde fonce sur le Nécromancien (`Distance` inversée, même idiome que Gobelin→Nécromancien dans `IA Gobelin`, sûr ici car le Nécromancien est toujours une instance unique) et l'attaque à 40px (`DegatsHorde`=40, cooldown 0.5s — tue le Nécromancien (PV 100) en 2-3 coups), texte de dégâts réutilisé tel quel. Pas de `ForEach`/`PickNearest` nécessaire (cible toujours singleton, contrairement à Gobelin↔Villageois).
- **Nouvel objet UI `TxtFenetreSortie`** (calque Debug, sous `TxtPVNecro`, x=20/y=170) — même pattern que les textes de statut existants.
- **Nouvelles variables globales** : `FenetreSortieActive`, `FenetreSortieTemps`, `FenetreSortieDuree`(10), `DegatsHorde`(40), `CooldownAttaqueHordeDuree`(0.5).
- Zéro nouvelle instruction non vérifiée (uniquement des instructions déjà de la table du skill) — la seule nouveauté est l'usage de `Sortie.X()`/`Sortie.Y()` comme base d'expression pour un `Create`, extension directe du pattern déjà confirmé avec `Necromancien.X()`/`Y()`.
- `docs/structure_json_projet_x.md` mis à jour (objets, groupes #18/#19, variables globales).
- **Testé et validé par Arnaud dans l'éditeur** : timer, vague ponctuelle, spawn de la horde à expiration, Game Over, affichage `TxtFenetreSortie` et rendu du placeholder `horde_placeholder.png` — tout fonctionne. Concept de la fenêtre de sortie/horde jugé bon par Arnaud ; chiffres (durée, dégâts, tailles de vague/horde) explicitement ouverts à ajustement ultérieur, pas figés. **Boucle complète du niveau 1 fermée** (dernier morceau du chiffrage niveau 1, voir "Design concret des 3 niveaux" dans `CONCEPT_JEU.md`).

## Prochaine session

- Vibration visuelle en combat pour un Villageois qui frappe le Nécromancien (actuellement câblée uniquement pour le combat contre un Gobelin — `Vibration Villageois` gated sur `Ciblé`/Gobelin, pas encore générique) — petit polish visuel, pas bloquant.
- Nom définitif du jeu : point ouvert dans `CONCEPT_JEU.md`, priorité basse, pas bloquant.
- Optionnel si ça gêne en pratique : empêcher aussi le chevauchement Gobelin/Necromancien (actuellement seule la séparation Gobelin/Gobelin existe).
- ~~Priorité 1 : valider palette + lancer 1ère génération de décor~~ **Fait (session 7, voir plus bas)** — herbe/chemin en jeu, validé par Arnaud.
- ~~Priorité 1 (mise à jour, fin session 7) : décider si on teste PixelLab Tier 1~~ **Tranché (session 8) : oui, test lancé.**
- **Priorité 1 (mise à jour, session 8)** : une fois l'abonnement PixelLab payé par Arnaud, tester `create_topdown_tileset` (nouvelle session requise pour que l'outil MCP soit visible) sur une seule transition simple (ex. herbe/chemin) avant de généraliser — comparer au résultat déjà obtenu à la main (9 tuiles intégrées et fonctionnelles dans `NewTileMap`) avant de décider de remplacer ou compléter la méthode manuelle. Si le test ne convient pas ou que l'abonnement n'est pas gardé : continuer à peindre le reste de la carte à la main (méthode déjà validée), puis attaquer une maison Base/Sommet (premier objet de décor avec hauteur/YSort, pas encore fait).

**Suite (2026-07-23, session 7) — démarrage du décor réel : méthode de production documentée.**
- **Nouveau doc `docs/PIPELINE_DECOR.md`** : adaptation de la méthode de production d'assets de The Long Ride (persona Nano Banana Pro "Concepteur de tiles", fondamentaux pixel art, Base+Sommet+YSort) — référencée par pointeur vers `Desktop/The Long Ride/PROJECT/docs/actif/`, jamais dupliquée, conforme à `<relation-the-long-ride>` de `CLAUDE.md`.
- **2 décisions actées par Arnaud** (2 questions posées avant d'écrire le doc) : (1) intégration GDevelop **directe** (Sprites/Tilemap natif, pas de Tiled externe comme TLR — chaîne non prouvée même côté TLR, inutile à l'échelle d'une seule scène) ; (2) palette de Projet X à définir à partir de références qu'Arnaud apporte (pas une reprise de la palette TLR — ton différent).
- **Référence de style transmise par Arnaud** : image de village TLR existant (même vue ¾, très dense) comme cible de cadrage/caméra — demande explicite d'un rendu **"beaucoup plus dégagé"** (moins dense, espace de jeu ouvert pour le combat/les hordes) et **style médiéval-fantasy** (pas cyclisme), ton **chaleureux/héroïque** — le Nécromancien (joueur) est la menace qui vient perturber ce village, pas l'inverse.
- **Découverte en cours de route** : `PROJECT/Assets/Necromancien.jpg` existait déjà (référence de style du personnage, violet/or/noir, jamais utilisée dans le jeu jusqu'ici — le placeholder actuel en jeu est un simple carré violet). Sert de palette du **personnage**, distincte de la palette de l'**environnement** (même séparation que TLR section 1bis) — le contraste chaleur-du-village vs noirceur-du-nécromancien vient de deux palettes séparées, pas d'un mélange.
- **Palette candidate extraite** de l'image de référence du village (script PowerShell, échantillonnage + quantification par fréquence, même méthode technique que TLR) — 4 groupes (toits/chaleur, végétation, pierre/bois, accents sombres), documentée dans `PIPELINE_DECOR.md` section 4. ⚠️ **Brouillon, pas encore validé par Arnaud.**
- **Question tranchée : décor modulaire, jamais une image de scène entière posée comme fond.** Justifié par 3 besoins déjà réels du projet, pas une préférence abstraite : Y-sort dynamique (Nécromancien/Gobelins/Villageois doivent passer devant/derrière maisons et arbres), collision (obstacles défensifs niveau 2 déjà prévus), renforts sortant de maisons identifiables (idée déjà notée dans "Ouvert" ci-dessus). Le sol pur (herbe, pavés, chemin) peut rester en chunks plus grands que 32×32 (comme les chunks de route TLR) puisqu'aucune verticalité n'y est mêlée — tout ce qui a une hauteur ou une fonction de gameplay reste un objet séparé.
- Rien généré ni intégré dans GDevelop à ce stade — cette session pose la méthode et le brouillon de palette, la génération réelle attend la validation de la palette par Arnaud.
- **Palette validée par Arnaud** ("ça me va"), style confirmé médiéval-fantasy. `PIPELINE_DECOR.md` complété avec la persona de génération ("Concepteur de tiles — Projet X", section 5) et un premier prompt concret prêt à l'emploi (section 6 : planche de tuiles de sol — herbe/chemin/pavés). Rien encore généré par Arnaud à ce stade — prochaine étape : lancer ce prompt, passer le résultat par Tileset Forge, puis intégrer dans `game/assets/`.

**Suite (2026-07-23, même session) — 1ère génération testée, blocage identifié : pas de grammaire de rôles de tuiles.**
- Arnaud a lancé le prompt (section 6) et passé le résultat par Tileset Forge → export `tileset_individual.zip` (25 tuiles + cartes normales). Revue par Claude Code (planche de contact assemblée pour inspection groupée) : constat partagé avec Arnaud, **pas intégré dans le jeu**.
- **3 problèmes concrets identifiés** :
  1. **Trop de détail/texture fine par tuile** (grain du chemin de terre, texture d'herbe) pour l'échelle d'affichage réelle (32px en jeu) — lisibilité en pixel art demande des blocs de couleur plus francs, pas du bruit haute fréquence (rejoint `REFERENCE_TECHNIQUE_PIXEL_ART.md` section 4 : "blocs de couleur, jamais de pixels isolés/aléatoires").
  2. **Pas de jeu de rôles systématique** : la génération a produit ~24 variations proches (croix, T, coins) de façon quasi aléatoire plutôt qu'un set minimal couvrant délibérément chaque cas de transition (bord N/S/E/O, coin intérieur/extérieur, croix, isolé) — un vrai système d'autotiling (blob tileset/wang tiles) désigne précisément quel rôle chaque tuile occupe avant de générer, pas après.
  3. Une tuile hors-palette/hors-style (pavés gris générique, ne correspond pas à la palette validée) et une tuile avec un artefact de génération (coin découpé/transparent) — attendu à ce stade de curation, pas un échec de méthode.
- **Blocage nommé explicitement par Arnaud comme récurrent** (déjà rencontré sur The Long Ride, jamais résolu) : comprendre comment définir *à l'avance* le rôle que doit jouer chaque tuile d'un set, plutôt que générer en masse et trier après coup.
- **Décision** : pause de la génération. Arnaud part sur une séance de transcription YouTube dédiée à la **grammaire/règle qui encadre la conception d'un set de tuiles** (rôles, autotiling, pas la génération d'image elle-même) avant de relancer une génération ciblée. Prompt/persona de `PIPELINE_DECOR.md` restent valides pour la partie génération — ce qui manque est la définition du set de rôles à demander, en amont.

## Ouvert — pas pour maintenant (suite)

- ~~**Grammaire des rôles de tuiles non maîtrisée**~~ **Résolu (2026-07-23, même session)** — voir suite ci-dessous.

**Suite (2026-07-23, même session) — grammaire de tuiles résolue, première planche produite et intégrée en jeu.**
- **4 transcriptions vidéo étudiées** (`Transcription vidéos/`, résumées en détail dans `PIPELINE_DECOR.md` section 4-5) : autotile Godot (bitmask 3×3, Match Corner Inside), méthode d'inpainting progressif PixelLab (non utilisable en l'état, outil séparé payant), comparatif Dual-Grid (terminologie standard 15/47/16 pièces, confirme notre choix), grammaire de structures linéaires (murs/clôtures, pas un besoin actif). Blocage de la session précédente résolu : grammaire = 2 bases + 4 bords + coins (14 tuiles complet ou 10 simplifié, coins partagés entre intérieur/extérieur).
- **Décision confirmée par Arnaud : version à 10 tuiles** (pas 14) — compromis qualité/vitesse expliqué et accepté, cohérent avec "finir vite".
- **2 générations réelles lancées** : 1ère (8 tuiles utilisables sur 11 demandées, moitié de la planche perdue en swatches de couleur à cause d'une liste de hex codes redondante dans le prompt — palette déjà dans la persona système) ; 2e génération de complément (2 bords manquants obtenus propres, coin "Bottom-Left" jugé incohérent par Arnaud avec le reste du set — **écarté volontairement**, bon réflexe de curation). Résultat final retenu : 9 fichiers (2 bases, 4 bords, 2 coins diagonaux — 2 coins suffisent, les 2 autres orientations s'obtiennent par flip dans GDevelop).
- **Import** : 9 tuiles renommées explicitement (`sol_herbe.png`, `sol_chemin.png`, `sol_bord_haut/bas/gauche/droite.png`, `sol_coin_a/b.png`, `sol_paves.png`) et copiées dans `game/assets/`.
- **Intégration GDevelop** (détail complet + schémas JSON vérifiés dans le skill `gdevelop-operations` et `docs/structure_json_projet_x.md`) :
  - 9 objets Sprite créés (dossier `Décor`) — **bug trouvé et corrigé** : image référencée dans un objet Sprite ne suffit pas, il faut aussi déclarer le fichier dans le manifeste `resources` en tête du JSON (sinon placeholder violet "G" cassé). Réflexe capitalisé dans le skill.
  - Tentative de fond d'herbe via `TiledSpriteObject::TiledSprite` (`SolHerbeFond`) — **retirée** après un vrai problème en jeu (ne partage pas de grille avec `TileMap::SimpleTileMap`, désalignement visuel avec le chemin peint par Arnaud).
  - Arnaud a créé un objet `TileMap::SimpleTileMap` (`NewTileMap`) dans l'éditeur pour peindre le chemin au pinceau — atlas `sol_atlas.png` assemblé (planche 3×3 générée par script PowerShell `System.Drawing` à partir des 9 fichiers). **Schéma du plan de tuiles par instance découvert et vérifié** (JSON interne dans `stringProperties`, index brut de l'atlas, `-1`=case vide — capitalisé dans le skill).
  - **Correction finale** (après un 1er test en jeu par Arnaud) : instance de chemin recalée sur la grille 32px (était à un offset non multiple de 32, cause du désalignement) ; nouveau calque **`Sol`** créé sous le calque de base (empêche les tuiles de cacher les objets de gameplay, indépendamment du zOrder) ; le fond d'herbe est maintenant une **2e instance du même objet `NewTileMap`** (remplissage uni 56×32 de la tuile herbe) plutôt que le `TiledSprite` retiré — même grille que le chemin, alignement garanti par construction.
  - **Testé et validé par Arnaud dans l'éditeur** : herbe et chemin s'emboîtent bien, objets de gameplay restent visibles par-dessus.
  - Commit + push faits (`9d31d62`).
- **Nouvelle piste ouverte, pas tranchée** : Arnaud a vu une vidéo courte montrant PixelLab générer un modèle pixel art à partir d'un dessin grossier de zones. Recherche menée : outil dédié **"Create Tileset"** (+ "Create Texture" en amont) qui automatiserait la grammaire de transition qu'on vient de construire à la main, avec export direct en Wang/dual-grid/3×3 tileset. **Nécessite un abonnement PixelLab Tier 1 (12$/mois, séparé de Sorceress)** — documenté dans `Desktop/The Long Ride/PROJECT/docs/actif/REFERENCE_PIXELLAB.md` section 6quater (référence partagée). Aucun test réel, décision financière/essai encore à prendre par Arnaud.

**Suite (2026-07-23, session 8) — connexion MCP PixelLab configurée, abonnement pas encore payé.**
- **Décision prise par Arnaud** : tester PixelLab Tier 1 (12$/mois — capture d'écran confirmée : 2000 images/mois, génération de maps incluse, licence commerciale) un mois, résiliable si ça ne convient pas. Motivation : Sorceress produit des tuiles cohérentes entre elles mais l'usage/tri reste difficile pour Arnaud.
- **Découverte clé sur `pixellab.ai/mcp`** : PixelLab expose un serveur MCP branchable directement sur Claude Code, avec un outil `create_topdown_tileset(lower=..., upper=..., ...)` qui génère nativement un **tileset Wang complet** (grammaire bords/coins/croix) — potentiellement redondant avec le travail manuel de grammaire de tuiles déjà résolu (voir [[feedback_tile_grammar_blocker]]), à valider si le résultat colle au style/palette déjà validés dans `PIPELINE_DECOR.md`.
- **Connexion technique déjà faite** : `claude mcp add pixellab https://api.pixellab.ai/mcp -t http -H "Authorization: Bearer <token perso Arnaud>"` exécuté, scope **local à ce projet** (`C:\Users\ROG\.claude.json`, section Projet X), confirmé `✔ Connected` via `claude mcp list`. **Les outils PixelLab ne sont visibles qu'à partir d'une nouvelle session** (pas dans la session où le serveur a été ajouté).
- **Bloquant avant test réel** : abonnement PixelLab pas encore payé par Arnaud. Sans paiement, les appels d'outils MCP échoueront probablement (quota/plan gratuit très limité).

**Suite (2026-07-23, session 8) — Tier 1 payé, méthode Sorceress déclarée obsolète, scène v1 complète construite.**
- **Abonnement Tier 1 payé** (2000 générations/mois confirmées). Test `create_building_kit` (maison modulaire, 56 pièces) : **abandonné** — pas de convention d'ancrage documentée côté PixelLab, aperçu composite construit à la main montrant de vrais espaces entre pièces. Remplacé par des maisons **pré-composées** en une image via `create_map_object` (même méthode que le puits/tonneau/étal).
- **Leçon de style capitalisée** : `view: "low top-down"` (~20°) en mode basique donne un rendu plat cohérent pour bâtiments/props ; `"high top-down"` (~35°) dérive vers un rendu plus isométrique sur les toits — `low top-down` retenu comme défaut. Palette : ne jamais fournir un `background_image` qui ne contient pas les couleurs du matériau à générer (contexte "herbe seule" a produit des murs sombres non voulus une première fois) ; description textuelle explicite ("no dark or black colors") suffit en mode basique.
- **Incident critique : projet GDevelop corrompu et inouvrable**, résolu et documenté dans le skill `gdevelop-operations` (2 causes cumulées) : (1) édition du JSON pendant que GDevelop avait le projet ouvert (collision avec sa sauvegarde automatique) — **règle actée : toujours vérifier/faire fermer GDevelop avant toute édition directe du JSON** ; (2) `Set-Content -Encoding UTF8` en PowerShell ajoute un BOM que GDevelop ne sait pas lire (message d'erreur générique, pas une erreur JSON explicite) — **règle actée : toujours écrire via `[System.IO.File]::WriteAllText` avec `UTF8Encoding($false)`, jamais `Set-Content -Encoding UTF8` sur ce fichier.**
- **Herbe↔pavés généré et chaîné** sur la même tuile d'herbe que herbe↔chemin (`upper_base_tile_id`, technique confirmée par la transcription PixelLab "Generating pixel art tilesets") — cohérence garantie entre les deux tilesets.
- **Nettoyage complet demandé par Arnaud et fait** : suppression des 9 objets Sprite Sorceress (SolHerbe, SolChemin, SolBord×4, SolCoinA/B, SolPaves) + `RepereDecor` (8 instances) + tous les fichiers PNG obsolètes (Sorceress + kit de bâtiment modulaire abandonné + fichiers de travail intermédiaires).
- **Scène v1 complète conçue et peinte par Claude Code en autonomie** (Arnaud : *"tu as réussi à bien lire et peindre en autonomie"*) : réseau de sol entier recalculé par script utilisant les métadonnées exactes des tilesets (corners Wang), pas de tuiles devinées. Route principale est-ouest + branche sud vers le cluster2 de Villageois, place pavée bordée au nord (puits, tonneau, étal de marché dedans). 3 maisons placées près des points d'intérêt existants (clusters de Villageois). Détail complet (positions, index de tuiles) dans `structure_json_projet_x.md`. Aperçus visuels générés dans `game/assets/apercu_scene_sol.png` et `apercu_scene_complete.png`.
- **Testé et validé par Arnaud** : le projet s'ouvre, tout s'affiche correctement.
- **Ouvert, priorités données par Arnaud pour la suite** : retours à venir d'Arnaud sur la scène v1 pour orienter les prochaines générations (assets manquants à identifier). Pas encore fait : découpage Base/Sommet+YSort sur les maisons (le Nécromancien passe toujours devant, jamais derrière, cf. `REGLES_PROFONDEUR_CALQUES_2D.md` de TLR).
